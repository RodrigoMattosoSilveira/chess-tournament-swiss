// All the tournament middleware tests are covered in util.tests.ts!
describe('Tournament Middleware Unit Tests', () => {
	it('Tournament Middleware canonical unit test', async done => {
		expect(1 + 1).toEqual(2);
		done();
	});
});