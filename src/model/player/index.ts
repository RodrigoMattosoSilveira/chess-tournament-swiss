import { PlayerController } from './playerController';
import { PlayerRouter as playerRouter } from './playerRouter';

const playerController = new PlayerController();

export { playerController };
export { playerRouter };
